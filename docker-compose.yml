version: '3.8'

services:
  klepp-api:
    build:
      context: .
      dockerfile: dev/app/Dockerfile
    volumes:
      - ${PWD}/app:/app:Z
    ports:
      - 127.0.0.1:8004:8004
    env_file: ${PWD}/.env
    environment:
      - ENVIRONMENT=dev
      - TESTING=1
    depends_on:
      - klepp-postgres-db

  klepp-postgres-db:
    build:
      context: .
      dockerfile: dev/db/Dockerfile
    expose:
      - 5432
    env_file: ${PWD}/.env
